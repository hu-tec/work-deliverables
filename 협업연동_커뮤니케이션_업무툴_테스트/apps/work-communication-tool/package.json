{
  "name": "work-communication-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Local SQLite BO work communication tool prototype.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
